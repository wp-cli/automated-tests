# behat.yml
default:
  suites:
    # WP-CLI bundle
    wp-cli/wp-cli-bundle:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/wp-cli-bundle/features

    # WP-CLI framework
    wp-cli/wp-cli:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/wp-cli/features

    # wp ability
    wp-cli/ability-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/ability-command/features

    # wp block
    wp-cli/block-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/block-command/features

    # wp cache
    wp-cli/cache-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/cache-command/features

    # wp checksum
    wp-cli/checksum-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/checksum-command/features

    # wp config
    wp-cli/config-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/config-command/features

    # wp core
    wp-cli/core-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/core-command/features

    # wp cron
    wp-cli/cron-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/cron-command/features

    # wp db
    wp-cli/db-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/db-command/features

    # wp embed
    wp-cli/embed-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/embed-command/features

    # wp comment | wp menu | wp network | wp option | wp post | wp site | wp taxonomy | wp term | wp user
    wp-cli/entity-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/entity-command/features

    # wp eval
    wp-cli/eval-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/eval-command/features

    # wp export
    wp-cli/export-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/export-command/features

    # wp plugin | wp theme
    wp-cli/extension-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/extension-command/features

    # wp i18n
    wp-cli/i18n-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/i18n-command/features

    # wp import
    wp-cli/import-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/import-command/features

    # wp language
    wp-cli/language-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/language-command/features

    # wp maintenance-mode
    wp-cli/maintenance-mode-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/maintenance-mode-command/features

    # wp media
    wp-cli/media-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/media-command/features

    # wp package
    wp-cli/package-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/package-command/features

    # wp rewrite
    wp-cli/rewrite-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/rewrite-command/features

    # wp role
    wp-cli/role-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/role-command/features

    # wp scaffold
    wp-cli/scaffold-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/scaffold-command/features

    # wp search-replace
    wp-cli/search-replace-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/search-replace-command/features

    # wp server
    wp-cli/server-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/server-command/features

    # wp shell
    wp-cli/shell-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/shell-command/features

    # wp super-admin
    wp-cli/super-admin-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/super-admin-command/features

    # wp widget
    wp-cli/widget-command:
      contexts:
        - WP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/wp-cli/widget-command/features
